[package]
name = "probash-desk"
version = "0.1.0"
edition = "2021"
description = "ProbashOS assisted-service desk (operator app)"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
