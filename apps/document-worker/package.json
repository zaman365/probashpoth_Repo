{
  "name": "@probash/document-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "WRANGLER_LOG_PATH=.wrangler/logs wrangler deploy --dry-run --outdir dist",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20260515.1",
    "@types/pg": "^8.23.1",
    "typescript": "6.0.3",
    "vitest": "^4.1.11",
    "wrangler": "4.92.0"
  }
}
